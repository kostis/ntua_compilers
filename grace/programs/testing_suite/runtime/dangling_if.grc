fun k(): nothing
{
    if 1 = 1 then
        if 1 = 2 then 
            writeInteger(2);
        $ Dangling if, the else stmt will be matched to the closest if in out implementation
        $ Expected: 4 should be printed
        else  writeInteger(4);
}