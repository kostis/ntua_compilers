fun p(): nothing
    var y : int;
    fun f(x : int) : int
    {
        if x > 0 then return 0;

        writeString("Hello\n");
    }

{
    y <- f(1000);
    y <- f(-1);
    y <- f(-1);
}

$ Hello, Hello